---
title: Hello code
---

# Hello code

~~~
// Fenced code block with Markdoc brackets {} and multiple lines
const obj = { key: 'value' };
console.log(`Hello, {world}! Template: ${obj.key}`);
function test() {
  return { nested: true };
}
~~~

this `some code` is an attribute {% class="25% {}" %}
